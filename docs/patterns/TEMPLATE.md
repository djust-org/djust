# <Class name>

**Rule** (the line the rule sheet carries): <one sentence, imperative>.
→ this page

**Status**: `skill` | `wiki-only` · **gate**: none | `<script / test / checklist row>` · **candidate for mechanical gate**: yes | no
**Origin**: <PR / issue / retro that first named the class>

## Instances

| PR | what drifted / what was missed | cure | rule in force? |
|---|---|---|---|
| #NNN | | | no (origin) |
| #NNN | | | yes, fired |
| #NNN | | | yes, missed |

`rule in force?` is the column `/pipeline-retro` Stage 3.7 reads: `fired` and `missed`
are counted per milestone from this table. Use `no (origin)` when the rule did not yet
exist — do not record a miss for a rule that was not there to fire.

## Detection

What a reviewer or a test can look for. Name the structural pins that already exist.

## Rejected shapes

Fixes tried for this class that did not hold, with the PR. Read this before proposing again.
