- **`test_differential_reachability_manifest_2345.py` runs the corpus sweep and the manifest once per DISTINCT input instead of once per reader** (#2723). Five cases each re-ran the full ~415,000-cell sweep on the unmutated script and differed only in what they read from the result — 36% of the suite's recorded time in one file — and the module-scoped `manifest` fixture was recomputed by every xdist worker that drew one of its cases. A `CorpusCache` keyed on the script's text, the `_rust` build digest and the argv now shares each artifact across the module and across xdist workers via the session's basetemp; a mutated copy is a different text and keeps its own run, and two canaries applying the identical mutation share one. No assertion changed; serial wall clock for the file 371 s -> 103 s (median of 3).
