- The theme gallery storybook is docs-first: a two-level topbar with the framework's theme switcher (preset + light/dark) on every page; a sidebar with a `/` search shortcut, category counts and a session-backed "Recently viewed" group that collapses behind a menu button on narrow screens; index and category cards carry a one-line description and, for template components, a rendered thumbnail; each component page opens with a **playground** (chips for every enumerable example kwarg, re-rendered through the preview component so a click is a component-scoped patch), then the live examples with their arguments folded away, copyable usage, one props table with a Required column, accessibility, slots, a collapsed source-and-styles panel, previous/next links in sidebar order and an "On this page" rail. Styles moved out of the templates into `djust_theming/css/storybook.css`; the only script is presentation (`storybook.js`).
