- **`djust init --dry-run` labelled the settings and ASGI rows `done` (#3172).**
  The table under "Dry run: nothing was written" read `done  djust block
  appended` and `done  replaced Django's default`. Those rows now read `would
  change` with what would happen (`append djust block`, `replace Django's
  default`, `create`). Rows that are `unchanged` or need attention keep their
  status, and a real run still reports `done`.
