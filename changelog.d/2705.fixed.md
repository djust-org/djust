- Flush pending handler and element rate limits through HTTP keepalive during page teardown and WebSocket close, coalescing edits to the latest payload. Reset rate-limit, cache, and optimistic configuration on every WebSocket/SSE mount so old-view timers and rules cannot affect the next view.
