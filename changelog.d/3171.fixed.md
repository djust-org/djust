- **System check `djust.T012` now recognises `dj-root` on the root element (#3171).**
  The warning no longer fires when `dj-root` is present without `dj-view`, matching the documented LiveView pattern where `dj-root` is authored and `dj-view` is injected at runtime. The warning message and hint now recommend adding `dj-root`.
