- **The version-sync check now covers the shipped SBOM (#3184).**
  `scripts/check-lockfile-versions.py` (run by `make release`, the CI test job
  and a pre-commit hook) now also fails when the root `djust` component of
  `python/djust/djust.cdx.json` names a version other than `pyproject.toml`'s.
  The 1.3.0rc4 cut needed a manual commit because the SBOM still said rc3.
  `make version` already regenerates the SBOM (added with it in #3136); a test
  now pins that. New cases in `TestSbomVersion`.
